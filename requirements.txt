# Core dependencies
rich>=12.0.0        # CLI Dashboard and rich text formatting
psutil>=5.9.0       # System monitoring (CPU, memory, temperature)

# Optional dependencies
pydantic>=1.9.0     # Configuration validation
